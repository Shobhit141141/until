PORT=3000
# NODE_ENV=development  # Dev only: server sends correctIndex so client can show correct answer in quiz
MONGO_URI=mongodb://localhost:27017/until
STACKS_API_URL=https://api.testnet.hiro.so
STACKS_RECIPIENT_ADDRESS=ST2CY5VNHNHVCVBYMM7Z2N2X0X2FF40ME2SPYJ8F9
# For withdrawals: platform wallet private key (hex). Must hold STX.
# STACKS_SENDER_SECRET_KEY=your_private_key_hex
# STACKS_NETWORK=testnet
# REDIS_URL=redis://localhost:6379
QUESTION_PRICE_MICRO_STX=1000000

GEMINI_API_KEY=your_gemini_key
GEMINI_MODEL=gemini-1.5-flash

MISTRAL_API_KEY=your_mistral_key
MISTRAL_MODEL=mistral-small-latest

# Scaling factor K (volume knob). Default 100; 500–2000 for real USD (~$0.25 L1 best-case at K=1000).
# SCALE_K=1000

# Milestone incentive pool (STX at K=100). 70% completion = pool/4; 100% = full pool.
BONUS_POOL_STX=4.5

# When true, disable AI; use only static questions from server/src/data/questions/*.json.
# USE_STATIC_QUESTIONS=true

# Feedback (nodemailer). If unset, feedback form still returns 200 but no email is sent.
# Port 587 = STARTTLS (recommended). Port 465 = implicit TLS. Do not use SMTP_SECURE with 587.
# FEEDBACK_EMAIL_TO=your@email.com
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
