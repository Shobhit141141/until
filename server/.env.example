PORT=3000
MONGO_URI=mongodb://localhost:27017/until
STACKS_API_URL=https://api.testnet.hiro.so
# Required: valid Stacks address (testnet: ST2..., mainnet: SP2...). Get one from Leather/Xverse.
STACKS_RECIPIENT_ADDRESS=ST2CY5VNHNHVCVBYMM7Z2N2X0X2FF40ME2SPYJ8F9

# Optional: Redis URL for challenge store (survives restarts, shared across processes). If unset, in-memory store is used.
# REDIS_URL=redis://localhost:6379

# Question payment (micro-STX; 1 STX = 1_000_000)
QUESTION_PRICE_MICRO_STX=1000000

# Gemini (question generation, primary)
GEMINI_API_KEY=your_gemini_key
GEMINI_MODEL=gemini-1.5-flash

# Mistral (fallback when Gemini rate-limited; optional)
MISTRAL_API_KEY=your_mistral_key
MISTRAL_MODEL=mistral-small-latest

# Scaling factor K (volume knob). Default 100; 500–2000 for real USD (~$0.25 L1 best-case at K=1000).
# SCALE_K=1000

# Milestone incentive pool (STX at K=100). Target 10–15% of expected full-run spend. 70% = X/4; 100% = X.
BONUS_POOL_STX=0.0125
